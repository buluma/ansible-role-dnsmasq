# test file
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: Include vars
      include_vars: "{{ playbook_dir }}/vars/main.yml"
  roles:
    - ../../
  post_tasks:
    - name: Include tasks
      include: "{{ playbook_dir }}/tasks/post.yml"
